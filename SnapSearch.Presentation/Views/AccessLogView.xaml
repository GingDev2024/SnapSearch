<UserControl x:Class="SnapSearch.Presentation.Views.AccessLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </UserControl.Resources>

    <Grid Background="{StaticResource PrimaryBrush}" Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Access Logs" Style="{StaticResource SectionHeader}" Margin="0" />
            <Button Content="Export CSV" Style="{StaticResource SecondaryButton}"
                    HorizontalAlignment="Right" Command="{Binding ExportCsvCommand}" Padding="12,6" />
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="FROM" Style="{StaticResource FormLabel}"
                           VerticalAlignment="Center" Margin="0,0,8,0" />
                <DatePicker Grid.Column="1" SelectedDate="{Binding FromDate}"
                            Background="{StaticResource SurfaceLightBrush}" Foreground="Black" />
                <TextBlock Grid.Column="3" Text="TO" Style="{StaticResource FormLabel}"
                           VerticalAlignment="Center" Margin="0,0,8,0" />
                <DatePicker Grid.Column="4" SelectedDate="{Binding ToDate}"
                            Background="{StaticResource SurfaceLightBrush}" Foreground="Black" />
                <TextBox Grid.Column="6" Style="{StaticResource SearchTextBox}"
                         Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Column="8" Content="Load" Style="{StaticResource PrimaryButton}"
                        Command="{Binding LoadLogsCommand}" Padding="16,8" />
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid Style="{StaticResource StyledDataGrid}"
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeader}"
                      ItemsSource="{Binding Logs}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time"
                                        Binding="{Binding AccessedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" Width="150" />
                    <DataGridTextColumn Header="User"       Binding="{Binding Username}"      Width="120" />
                    <DataGridTextColumn Header="Action"     Binding="{Binding Action}"         Width="110" />
                    <DataGridTextColumn Header="Keyword"    Binding="{Binding SearchKeyword}"  Width="130" />
                    <DataGridTextColumn Header="IP Address" Binding="{Binding IpAddress}"      Width="120" />
                    <DataGridTextColumn Header="MAC Address" Binding="{Binding MacAddress}"    Width="140" />
                    <DataGridTextColumn Header="File / Details" Binding="{Binding Details}"   Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <Border Grid.Row="3" Padding="0,8,0,0">
            <Grid>
                <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource StatusText}" />
                <ProgressBar HorizontalAlignment="Right" Width="200" Height="4"
                             IsIndeterminate="True"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}"
                             Background="Transparent" Foreground="{StaticResource AccentBrush}" />
            </Grid>
        </Border>
    </Grid>
</UserControl>